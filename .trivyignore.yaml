secrets:
  - id: azure-storage-account-key
    paths:
      - dist/post/index.bundle.js
      - dist/post/index.bundle.js.map
    statement: >
      Well-known Azure Storage Emulator (devstoreaccount1) test key bundled from @azure SDK dependencies. Not a real secret.
